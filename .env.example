LOG_LEVEL=info

AGENT_NAME=Assistant

GENERATION_MODEL=gpt-4o-mini

# Typhoon OCR — OpenAI-compatible chat/completions endpoint for document extraction
TYPHOON_BASE_URL=
TYPHOON_OCR_API_KEY=
TYPHOON_OCR_MODEL=typhoon-ai/typhoon-ocr-7b

# Embeddings — BAAI/bge-m3 over a TEI-compatible /embed endpoint (HF TEI / Infinity)
TEI_EMBEDDING_BASE_URL=
TEI_EMBEDDING_API_KEY=
TEI_EMBEDDING_RPS=

# Reranking — BAAI/bge-reranker-v2-m3 over a TEI-compatible /rerank endpoint
TEI_RERANKER_BASE_URL=
TEI_RERANKER_API_KEY=
TEI_RERANKER_RPS=

# Embeddings — text-embedding-3-* over an OpenAI-compatible /embeddings endpoint
OPENAI_BASE_URL=
OPENAI_API_KEY=

# Vector store — one Qdrant collection per knowledge base
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=